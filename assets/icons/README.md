# Custom icons

Place PNG image files in this folder and point `icons.json` at them. 

- `app` — application/window/installer icon.
- `txt` — `.txt` documents.
- `md` — `.md` and `.markdown` documents.
- `bb` — `.bb`, `.bbcode`, and `.scratch` documents.

Missing files automatically fall back to the supplied `icon_256.png`.
